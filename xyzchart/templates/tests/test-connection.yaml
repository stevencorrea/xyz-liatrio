apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "xyzchart.fullname" . }}-test-connection"
  labels:
    {{- include "xyzchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "xyzchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
